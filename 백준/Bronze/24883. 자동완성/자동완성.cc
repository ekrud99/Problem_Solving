#include <iostream>

using namespace std;

int main() {
    
    string str;
    
    cin >> str;
    
    if(str == "N" || str == "n") cout << "Naver D2";
    else cout << "Naver Whale";

    return 0;
}