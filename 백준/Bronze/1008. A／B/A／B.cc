#include <bits/stdc++.h>
#define FAST ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL)
using namespace std;
int main(void){
    FAST;
    double a, b;
    cin >> a >> b;
    cout << fixed;
    cout.precision(9);
    cout << a / b;
    return 0;
}